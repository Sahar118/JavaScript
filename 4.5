const leapyearfunc = (year) => {
  if ((year % 4 === 0 && year % 100 !== 0) || year % 400 === 0) {
    console.log("it is needed a leap year.");
  } else {
    console.log("this is not a leap year.");
  }
};
